---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, children } = Astro.props;
---

<li class="link-card">
	<a href={href}>
		<h2>
			{title}
			<span>&rarr;</span>
		</h2>
		<p>
			{children}
		</p>
	</a>
</li>